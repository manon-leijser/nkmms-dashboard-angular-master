import { CircuitComponents } from "./circuit-components";

describe('CircuitComponent', () => {
    it('should create an instance', () => {
        expect(new CircuitComponents()).toBeTruthy();
    });
});